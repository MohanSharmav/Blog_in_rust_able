
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Titljjjjje</title>
</head>
<body>

<ul class="people_list">
    {{#each o}}
        <a href="{{this}}"><li>{{this}}</li></a>
    {{/each}}
</ul>

<ul class="people_list">
    {{#each p}}
        <li><a href="{{this}}"> {{this}}</li></a>
    {{/each}}
</ul>


{{q.[1].title}}
<p>
{{#each q}}
    {{#each this}}<
        <a href="{{this}}">  {{ this }}</a>
    {{/each}}<br>
{{/each}}


</p>
</ul>
</body>
</html>